(function($) { 
    // AJAX configuration
    $._captcha = {
        'temporalUrl': "${dhek.temporalCaptchaUrl}",
        'imageUrl': "${dhek.captchaImageUrl}",
        'checkUrl': "${dhek.captchaCheckUrl}"
    }
})(jQuery);
